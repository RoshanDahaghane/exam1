create table customer(
name varchar(30),
ph.no. varchar(30) unique,
email_id varchar(30) primary key
);

create table office (
loc varchar(30) primary key,
name varchar(30) 
);

create table counter(
name varchar(30),
cr.no. int primary key
);
create table canteen (
room no int ,
roomid int primary key()
)